管理以下檔案（初次使用時，請修改 paths.zsh 設定檔案位置。參考 paths-example.zsh ）

  vim files   使用者的 Vim 插件、語法等檔案（見 rsync-include）
              位置在 Vim 中 :echo $HOME 輸出的目錄之下
  vimrc       系統 vimrc，通常在 vim --version 顯示的 system vimrc file
  zshrc       系統 zshrc，通常在 /etc/zshrc
  gitconfig   系統 git config
  (g)vimrc    另外安裝 gVim 的 vimrc


使用：

  執行 update.sh      取得系統使用中的檔案，覆蓋此路徑內的檔案。
  執行 overwrite.sh   以此路徑檔案更新系統內的檔案（危險）。


已知問題：

  Vim plugin：gsession 與 ShowMarks 有按鍵衝突，
  故已於 .vimrc 設定 >
    let g:gsession_non_default_mapping = 1
< 取消 gsession 預設的 mapping。


Modelines: {{{1
vim:tw=78:ft=help
